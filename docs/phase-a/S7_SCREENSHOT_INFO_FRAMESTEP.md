# Phase A — S7 Screenshot / Media Info / FrameStep

## Delivered
- `FrameStep` ±1 UI + `,` / `.` hotkeys (pauses for still frame)
- `Screenshot` → SaveFileDialog → Facade `Screenshot(path)`
- Media Info panel from Facade `GetMediaInfo()` (mpv properties only: path/title/format/codecs/size/duration)
- F12 screenshot · Ctrl+I info

## Constraints
- No separate decoder / Media Foundation probe
- Blur OFF · no Settings search · no P1
